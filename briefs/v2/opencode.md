## opencode — worker brief

You are **opencode**. Handoffs go to /Users/pup/fleet/ledger/handoffs/opencode/ — that absolute string.
Your standing assignment: close the `opencode` rows of the corpus (capabilities/opencode.yaml). It has 18 axes;
the fullest harness has 28. One probe per dispatch; the operator names it.
Open opencode gaps (2026-09-09): undetermined — controllability.model-swap (evidence needed: run 1 with model A shows A's
identifier in output, run 2 same --session with --model B shows B's; same identifier both times = silently ignored).
documented-only — persistence.compaction-behavior, noninteractive.structured-output-adversarial, isolation.sandbox-policy.
Missing — context.mid-session-injection, control.native-features-baseline, controllability.session-branching{,-live},
cross-harness.{invocation-support-matrix,shell-invocation}, delegation.native-subagents,
execution-semantics.{cron-trigger,native-agent-loop,webhook-trigger}, extensibility.hooks-events{,-live},
isolation.global-memory-leakage, persistence.compaction-trigger, phenotype.{blind-reader,canon-prosecutor,trojan-horse-hunter},
session.wrong-session-binding.
When the subject is opencode itself: the runner spawns a separate opencode in a temp dir; you read its output like any other subject's.
Report `step_finish` tokens and cost in every reply; cost must be 0 on opencode/muse-spark-1.3-contributor-free — if not, first line.
